# square_numbers
# squares = []
# for value in range(1, 11):
#    square = value ** 2
#    squares.append(square)
#    squares.append(value**2)
# print(squares)

# min, max, sum
digits = [1, 2, 3, 4, 5, 6, 7, 8]
x = min(digits)
y = max(digits)
z = sum(digits)
print(x, y, z)